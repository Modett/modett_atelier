[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmd = "pnpm install --frozen-lockfile"

[phases.build]
cmd = "pnpm --filter @modett/db build && pnpm --filter @modett/api build"

[start]
cmd = "node apps/api/dist/server.js"